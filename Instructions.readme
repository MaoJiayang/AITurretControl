AI块火控炮塔系统 - 使用说明
==============================

本脚本通过 AI 攻击块 + AI 飞行块自动获取目标坐标，
控制网格上所有原版炮塔进行提前量火控射击。
无需手动编组或设置炮塔目标，放置即用。

【前置条件】
─────────────
1. 一个 AI 攻击块（Offensive Combat Block）
   - 保持开启（Enabled = On）
2. 一个 AI 飞行块（Flight Movement Block）
   - 保持关闭（Enabled = Off），脚本会自动配置
3. 至少一个驾驶舱 / 控制站
   - 用于获取舰船速度和重力信息
   - 可选：在名称中包含"主"字标签来指定主驾驶舱
4. 至少一个原版炮塔
5. 一个可编程方块，加载本脚本

【快速开始】
─────────────
1. 在网格上放置 AI 攻击块和 AI 飞行块各一个
2. 将本脚本复制到可编程方块中
3. 脚本会自动初始化：
   - 配置 AI 块工作模式
   - 扫描并识别所有炮塔
   - 按弹药类型和位置自动聚类分组
4. 当 AI 块锁定目标后，炮塔自动进行火控射击

【支持的炮塔类型】
─────────────
  炮塔类型          弹速    射程   重力影响  分类
  ──────────────────────────────────────────────
  加特林炮塔        800m/s  800m   是        机枪类
  机炮炮塔          800m/s  800m   是        机枪类
  室内炮塔          600m/s  600m   是        机枪类
  火箭炮塔          200m/s  800m   否        火炮类
  突击加农炮炮塔    500m/s  1400m  是        火炮类
  火炮炮塔          500m/s  2000m  是        火炮类

  默认仅托管火炮类炮塔。如需托管机枪类炮塔，
  在 CustomData 中设置: 托管机枪类 = True

【参数配置】
─────────────
所有参数通过可编程方块的 CustomData 读写，格式为：
  参数名 = 值

首次运行后 CustomData 会自动生成完整的参数列表。
修改参数后运行"重置"命令使其生效。

主要参数说明：

  主驾驶舱标签 = 主
    驾驶舱名称中包含此标签的将被用作主控制器。
    找不到时使用任意可用驾驶舱。

  聚类距离 = 12.5
    同类型炮塔在此距离（米）内共享火控计算结果。
    减小此值增加精度但消耗更多性能。

  火控更新周期 = 7
    所有聚类组在此周期（帧数）内至少更新一次火控。
    增大此值减少性能消耗但降低响应速度。

  弹道迭代次数 = 5
    弹道计算的迭代优化次数，越大越精确但越耗性能。

  每帧最大射击处理数 = 0.15
    每帧最多处理多少个炮塔的射击状态变更。
    支持小数（0.15 = 约每7帧处理1个），用于平滑性能。

  托管机枪类 = False
    设为 True 后脚本也会控制加特林/机炮/室内炮等机枪类炮塔。

  开启视线判定 = True
    检测炮塔到目标的射线是否被己方方块阻挡。
    如果被挡住则该炮塔不会开火，避免误伤。

  目标历史最大长度 = 4
    目标跟踪器保留的历史记录数量。
    影响预测模型的计算。

  方块更新间隔 = 3600
    每隔多少帧重新扫描网格上的炮塔列表。
    如果在战斗中动态添加/移除了炮塔，
    可以运行"刷新"命令立即更新。

【运行命令】
─────────────
在可编程方块的运行参数中输入以下命令：

  reset / 重置
    完全重新初始化所有组件，重新读取 CustomData 参数。
    修改参数后需要执行此命令。

  refresh / 刷新
    重新扫描炮塔列表，不重置其他状态。
    适用于战斗中增减了炮塔的情况。

  status / 状态
    刷新终端界面的状态显示。

【状态显示】
─────────────
可编程方块的终端界面会显示实时状态信息：
  - 当前火控状态（待机/跟踪目标/目标丢失）
  - 炮塔数量、聚类组数量、火炮类数量
  - 目标距离、预测误差
  - 预测模型权重（圆周/线性）
  - 性能统计（平均/最大耗时、指令数）
